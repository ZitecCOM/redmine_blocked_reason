<% if defined?(blocked_reason_sidebar) && !blocked_reason_sidebar.empty? && !@issue  %>
  <h3><%= l :blocked_reasons %></h3>
  <% @blocked_reason_sidebar.each do |reason| %>
    <%= render_sidebar_blocked_reason_type(reason) %>
  <% end %>
<% end %>
